# build artifacts
/uta
/dist/
*.test
*.out

# environment / secrets — never commit
.envrc
.env
.env.*
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json
.npmrc

# local databases / blobs (in case someone runs uta from this dir)
*.db
*.db-journal
*.db-wal
*.db-shm

# editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp

# AI-agent context files — kept local-only, never committed
CLAUDE.md
.claude/
.uta/
